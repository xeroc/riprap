/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  type Account,
  type Address,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
  transformEncoder,
} from "@solana/kit";
import {
  getPhaseDecoder,
  getPhaseEncoder,
  getTierDecoder,
  getTierEncoder,
  type Phase,
  type PhaseArgs,
  type Tier,
  type TierArgs,
} from "../types";

export const MUTUAL_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  69, 16, 41, 87, 137, 153, 87, 115,
]);

export function getMutualDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(MUTUAL_DISCRIMINATOR);
}

export type Mutual = {
  discriminator: ReadonlyUint8Array;
  /**
   * Initializer — demo admin; gates set_subaccord_param and the payout
   * pass-check co-signature.
   */
  authority: Address;
  /**
   * The pool this mutual owns (rights.authority = mutual_auth PDA,
   * ownership.authority = mutual_own PDA).
   */
  pool: Address;
  /** The Accord subaccord jurors stake into; authority = this Mutual PDA. */
  subaccord: Address;
  /** USDC for the pilot; contribution and payout mint. */
  depositMint: Address;
  /** USDC for the pilot; juror fees, claimant-funded. */
  feeMint: Address;
  /** Cover-terms document hash — the adjudicated reference (§9). */
  policyHash: ReadonlyUint8Array;
  /** The three cover tiers (policy doc: $10/$1k, $20/$2k, $40/$4k). */
  tiers: Array<Tier>;
  /** Coverage start; join reverts at/after it (§2.7). */
  depositsCloseAt: bigint;
  /** Coverage end plus reporting lag; file_claim reverts at/after it (§2.7). */
  claimsCloseAt: bigint;
  /** Duration after settlement; unpaid amounts revert to the residual (§2.5). */
  pullWindow: bigint;
  /**
   * Restated from the PDA seeds — the mutual PDA signs the create_dispute
   * CPI (and later pool CPIs), which requires seeds and bump at signing
   * time (pool `seed` precedent).
   */
  seed: bigint;
  phase: Phase;
  /** Set by settle_pool = settled_at + pull_window. */
  pullCloseAt: bigint;
  /** 1_000_000_000 = full pay; min(1e9, vault / (obligations + fee_refunds)). */
  ratio1e9: bigint;
  /** Σ approved claim_amounts. */
  obligations: bigint;
  /** Σ approved fees — refunds ride the same ratio (§2.6). */
  feeRefunds: bigint;
  claimsFiled: number;
  claimsResolved: number;
  /** Claim PDA nonce; one claim account per filing. */
  claimNonce: bigint;
  bump: number;
};

export type MutualArgs = {
  /**
   * Initializer — demo admin; gates set_subaccord_param and the payout
   * pass-check co-signature.
   */
  authority: Address;
  /**
   * The pool this mutual owns (rights.authority = mutual_auth PDA,
   * ownership.authority = mutual_own PDA).
   */
  pool: Address;
  /** The Accord subaccord jurors stake into; authority = this Mutual PDA. */
  subaccord: Address;
  /** USDC for the pilot; contribution and payout mint. */
  depositMint: Address;
  /** USDC for the pilot; juror fees, claimant-funded. */
  feeMint: Address;
  /** Cover-terms document hash — the adjudicated reference (§9). */
  policyHash: ReadonlyUint8Array;
  /** The three cover tiers (policy doc: $10/$1k, $20/$2k, $40/$4k). */
  tiers: Array<TierArgs>;
  /** Coverage start; join reverts at/after it (§2.7). */
  depositsCloseAt: number | bigint;
  /** Coverage end plus reporting lag; file_claim reverts at/after it (§2.7). */
  claimsCloseAt: number | bigint;
  /** Duration after settlement; unpaid amounts revert to the residual (§2.5). */
  pullWindow: number | bigint;
  /**
   * Restated from the PDA seeds — the mutual PDA signs the create_dispute
   * CPI (and later pool CPIs), which requires seeds and bump at signing
   * time (pool `seed` precedent).
   */
  seed: number | bigint;
  phase: PhaseArgs;
  /** Set by settle_pool = settled_at + pull_window. */
  pullCloseAt: number | bigint;
  /** 1_000_000_000 = full pay; min(1e9, vault / (obligations + fee_refunds)). */
  ratio1e9: number | bigint;
  /** Σ approved claim_amounts. */
  obligations: number | bigint;
  /** Σ approved fees — refunds ride the same ratio (§2.6). */
  feeRefunds: number | bigint;
  claimsFiled: number;
  claimsResolved: number;
  /** Claim PDA nonce; one claim account per filing. */
  claimNonce: number | bigint;
  bump: number;
};

/** Gets the encoder for {@link MutualArgs} account data. */
export function getMutualEncoder(): FixedSizeEncoder<MutualArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["authority", getAddressEncoder()],
      ["pool", getAddressEncoder()],
      ["subaccord", getAddressEncoder()],
      ["depositMint", getAddressEncoder()],
      ["feeMint", getAddressEncoder()],
      ["policyHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["tiers", getArrayEncoder(getTierEncoder(), { size: 3 })],
      ["depositsCloseAt", getI64Encoder()],
      ["claimsCloseAt", getI64Encoder()],
      ["pullWindow", getI64Encoder()],
      ["seed", getU64Encoder()],
      ["phase", getPhaseEncoder()],
      ["pullCloseAt", getI64Encoder()],
      ["ratio1e9", getU64Encoder()],
      ["obligations", getU64Encoder()],
      ["feeRefunds", getU64Encoder()],
      ["claimsFiled", getU32Encoder()],
      ["claimsResolved", getU32Encoder()],
      ["claimNonce", getU64Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: MUTUAL_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Mutual} account data. */
export function getMutualDecoder(): FixedSizeDecoder<Mutual> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["authority", getAddressDecoder()],
    ["pool", getAddressDecoder()],
    ["subaccord", getAddressDecoder()],
    ["depositMint", getAddressDecoder()],
    ["feeMint", getAddressDecoder()],
    ["policyHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["tiers", getArrayDecoder(getTierDecoder(), { size: 3 })],
    ["depositsCloseAt", getI64Decoder()],
    ["claimsCloseAt", getI64Decoder()],
    ["pullWindow", getI64Decoder()],
    ["seed", getU64Decoder()],
    ["phase", getPhaseDecoder()],
    ["pullCloseAt", getI64Decoder()],
    ["ratio1e9", getU64Decoder()],
    ["obligations", getU64Decoder()],
    ["feeRefunds", getU64Decoder()],
    ["claimsFiled", getU32Decoder()],
    ["claimsResolved", getU32Decoder()],
    ["claimNonce", getU64Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link Mutual} account data. */
export function getMutualCodec(): FixedSizeCodec<MutualArgs, Mutual> {
  return combineCodec(getMutualEncoder(), getMutualDecoder());
}

export function decodeMutual<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Mutual, TAddress>;
export function decodeMutual<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Mutual, TAddress>;
export function decodeMutual<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Mutual, TAddress> | MaybeAccount<Mutual, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getMutualDecoder());
}

export async function fetchMutual<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Mutual, TAddress>> {
  const maybeAccount = await fetchMaybeMutual(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeMutual<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Mutual, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeMutual(maybeAccount);
}

export async function fetchAllMutual(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Mutual>[]> {
  const maybeAccounts = await fetchAllMaybeMutual(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeMutual(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Mutual>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeMutual(maybeAccount));
}

export function getMutualSize(): number {
  return 330;
}
