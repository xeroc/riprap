/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findDepositorPda } from "../pdas";
import { POOL_PROGRAM_ADDRESS } from "../programs";

export const CRANK_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  0, 232, 3, 195, 124, 117, 105, 53,
]);

export function getCrankDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CRANK_DISCRIMINATOR);
}

export type CrankInstruction<
  TProgram extends string = typeof POOL_PROGRAM_ADDRESS,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountCranker extends string | AccountMeta<string> = string,
  TAccountDepositor extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountDestination extends string | AccountMeta<string> = string,
  TAccountTreasury extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountAssociatedTokenProgram extends string | AccountMeta<string> =
    "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPool extends string
        ? ReadonlyAccount<TAccountPool>
        : TAccountPool,
      TAccountCranker extends string
        ? ReadonlySignerAccount<TAccountCranker> &
            AccountSignerMeta<TAccountCranker>
        : TAccountCranker,
      TAccountDepositor extends string
        ? WritableAccount<TAccountDepositor>
        : TAccountDepositor,
      TAccountOwner extends string
        ? ReadonlyAccount<TAccountOwner>
        : TAccountOwner,
      TAccountDestination extends string
        ? WritableAccount<TAccountDestination>
        : TAccountDestination,
      TAccountTreasury extends string
        ? WritableAccount<TAccountTreasury>
        : TAccountTreasury,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountAssociatedTokenProgram extends string
        ? ReadonlyAccount<TAccountAssociatedTokenProgram>
        : TAccountAssociatedTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CrankInstructionData = { discriminator: ReadonlyUint8Array };

export type CrankInstructionDataArgs = {};

export function getCrankInstructionDataEncoder(): FixedSizeEncoder<CrankInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CRANK_DISCRIMINATOR }),
  );
}

export function getCrankInstructionDataDecoder(): FixedSizeDecoder<CrankInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCrankInstructionDataCodec(): FixedSizeCodec<
  CrankInstructionDataArgs,
  CrankInstructionData
> {
  return combineCodec(
    getCrankInstructionDataEncoder(),
    getCrankInstructionDataDecoder(),
  );
}

export type CrankAsyncInput<
  TAccountPool extends string = string,
  TAccountCranker extends string = string,
  TAccountDepositor extends string = string,
  TAccountOwner extends string = string,
  TAccountDestination extends string = string,
  TAccountTreasury extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountAssociatedTokenProgram extends string = string,
> = {
  pool: Address<TAccountPool>;
  /** The crank initiator; permissionless — anyone. Pays tx fees only. */
  cranker: TransactionSigner<TAccountCranker>;
  /** One depositor position of THIS pool; seeds bind pool and owner. */
  depositor?: Address<TAccountDepositor>;
  /**
   * The depositor's owner — receives the payout. Not a signer: cranking
   * someone else's position pays that someone, never the cranker.
   */
  owner: Address<TAccountOwner>;
  /**
   * Payout destination: the canonical ATA of the position's residual
   * payee — the owner, or the beneficiary when a funder sponsored the
   * position. Exactly one address passes: the assignment made at first
   * deposit is the contract, not a menu for whoever turns the crank.
   */
  destination: Address<TAccountDestination>;
  treasury: Address<TAccountTreasury>;
  tokenProgram?: Address<TAccountTokenProgram>;
  associatedTokenProgram?: Address<TAccountAssociatedTokenProgram>;
};

export async function getCrankInstructionAsync<
  TAccountPool extends string,
  TAccountCranker extends string,
  TAccountDepositor extends string,
  TAccountOwner extends string,
  TAccountDestination extends string,
  TAccountTreasury extends string,
  TAccountTokenProgram extends string,
  TAccountAssociatedTokenProgram extends string,
  TProgramAddress extends Address = typeof POOL_PROGRAM_ADDRESS,
>(
  input: CrankAsyncInput<
    TAccountPool,
    TAccountCranker,
    TAccountDepositor,
    TAccountOwner,
    TAccountDestination,
    TAccountTreasury,
    TAccountTokenProgram,
    TAccountAssociatedTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CrankInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountCranker,
    TAccountDepositor,
    TAccountOwner,
    TAccountDestination,
    TAccountTreasury,
    TAccountTokenProgram,
    TAccountAssociatedTokenProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? POOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: false },
    cranker: { value: input.cranker ?? null, isWritable: false },
    depositor: { value: input.depositor ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
    destination: { value: input.destination ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    associatedTokenProgram: {
      value: input.associatedTokenProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.depositor.value) {
    accounts.depositor.value = await findDepositorPda(
      {
        pool: getAddressFromResolvedInstructionAccount(
          "pool",
          accounts.pool.value,
        ),
        owner: getAddressFromResolvedInstructionAccount(
          "owner",
          accounts.owner.value,
        ),
      },
      { programAddress },
    );
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.associatedTokenProgram.value) {
    accounts.associatedTokenProgram.value =
      "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL" as Address<"ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("cranker", accounts.cranker),
      getAccountMeta("depositor", accounts.depositor),
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("destination", accounts.destination),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("associatedTokenProgram", accounts.associatedTokenProgram),
    ],
    data: getCrankInstructionDataEncoder().encode({}),
    programAddress,
  } as CrankInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountCranker,
    TAccountDepositor,
    TAccountOwner,
    TAccountDestination,
    TAccountTreasury,
    TAccountTokenProgram,
    TAccountAssociatedTokenProgram
  >);
}

export type CrankInput<
  TAccountPool extends string = string,
  TAccountCranker extends string = string,
  TAccountDepositor extends string = string,
  TAccountOwner extends string = string,
  TAccountDestination extends string = string,
  TAccountTreasury extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountAssociatedTokenProgram extends string = string,
> = {
  pool: Address<TAccountPool>;
  /** The crank initiator; permissionless — anyone. Pays tx fees only. */
  cranker: TransactionSigner<TAccountCranker>;
  /** One depositor position of THIS pool; seeds bind pool and owner. */
  depositor: Address<TAccountDepositor>;
  /**
   * The depositor's owner — receives the payout. Not a signer: cranking
   * someone else's position pays that someone, never the cranker.
   */
  owner: Address<TAccountOwner>;
  /**
   * Payout destination: the canonical ATA of the position's residual
   * payee — the owner, or the beneficiary when a funder sponsored the
   * position. Exactly one address passes: the assignment made at first
   * deposit is the contract, not a menu for whoever turns the crank.
   */
  destination: Address<TAccountDestination>;
  treasury: Address<TAccountTreasury>;
  tokenProgram?: Address<TAccountTokenProgram>;
  associatedTokenProgram?: Address<TAccountAssociatedTokenProgram>;
};

export function getCrankInstruction<
  TAccountPool extends string,
  TAccountCranker extends string,
  TAccountDepositor extends string,
  TAccountOwner extends string,
  TAccountDestination extends string,
  TAccountTreasury extends string,
  TAccountTokenProgram extends string,
  TAccountAssociatedTokenProgram extends string,
  TProgramAddress extends Address = typeof POOL_PROGRAM_ADDRESS,
>(
  input: CrankInput<
    TAccountPool,
    TAccountCranker,
    TAccountDepositor,
    TAccountOwner,
    TAccountDestination,
    TAccountTreasury,
    TAccountTokenProgram,
    TAccountAssociatedTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CrankInstruction<
  TProgramAddress,
  TAccountPool,
  TAccountCranker,
  TAccountDepositor,
  TAccountOwner,
  TAccountDestination,
  TAccountTreasury,
  TAccountTokenProgram,
  TAccountAssociatedTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? POOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: false },
    cranker: { value: input.cranker ?? null, isWritable: false },
    depositor: { value: input.depositor ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
    destination: { value: input.destination ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    associatedTokenProgram: {
      value: input.associatedTokenProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.associatedTokenProgram.value) {
    accounts.associatedTokenProgram.value =
      "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL" as Address<"ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("cranker", accounts.cranker),
      getAccountMeta("depositor", accounts.depositor),
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("destination", accounts.destination),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("associatedTokenProgram", accounts.associatedTokenProgram),
    ],
    data: getCrankInstructionDataEncoder().encode({}),
    programAddress,
  } as CrankInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountCranker,
    TAccountDepositor,
    TAccountOwner,
    TAccountDestination,
    TAccountTreasury,
    TAccountTokenProgram,
    TAccountAssociatedTokenProgram
  >);
}

export type ParsedCrankInstruction<
  TProgram extends string = typeof POOL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    pool: TAccountMetas[0];
    /** The crank initiator; permissionless — anyone. Pays tx fees only. */
    cranker: TAccountMetas[1];
    /** One depositor position of THIS pool; seeds bind pool and owner. */
    depositor: TAccountMetas[2];
    /**
     * The depositor's owner — receives the payout. Not a signer: cranking
     * someone else's position pays that someone, never the cranker.
     */
    owner: TAccountMetas[3];
    /**
     * Payout destination: the canonical ATA of the position's residual
     * payee — the owner, or the beneficiary when a funder sponsored the
     * position. Exactly one address passes: the assignment made at first
     * deposit is the contract, not a menu for whoever turns the crank.
     */
    destination: TAccountMetas[4];
    treasury: TAccountMetas[5];
    tokenProgram: TAccountMetas[6];
    associatedTokenProgram: TAccountMetas[7];
  };
  data: CrankInstructionData;
};

export function parseCrankInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCrankInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 8,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      pool: getNextAccount(),
      cranker: getNextAccount(),
      depositor: getNextAccount(),
      owner: getNextAccount(),
      destination: getNextAccount(),
      treasury: getNextAccount(),
      tokenProgram: getNextAccount(),
      associatedTokenProgram: getNextAccount(),
    },
    data: getCrankInstructionDataDecoder().decode(instruction.data),
  };
}
